# @swagger
# Settlement endpoints
/settlement/{tripId}:
  get:
    tags: [Settlement]
    summary: 取得結算結果
    parameters:
      - in: path
        name: tripId
        required: true
        schema:
          type: string
    responses:
      200:
        description: 結算結果
      404:
        description: 旅程不存在
